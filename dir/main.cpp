#include "../include/BSTree.hpp"

int main() {
    BSTree first;
    first.add_element(5);
    first.add_element(4);
    first.add_element(3);
    first.add_element(2);
    first.add_element(1);
    return 0;
}
